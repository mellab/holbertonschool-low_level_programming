#include <stdio.h>

/**
 * main - prints its name, followed by a new line
 * @argc: argument counter
 * @argv: argument array
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
